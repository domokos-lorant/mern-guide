export enum Routes {
    Home = "/",
    Cart = "/cart",
    Create = "/create",
    Account = "/account",
    Login = "/login",
    Signup = "/signup",
    Product = "/product"
}

export enum ApiRoutes {
    Cart = "api/cart",
    Checkout = "api/checkout",
    Orders = "api/orders",
    Users = "api/users",
    Account = "api/account"
}