export enum Routes {
    Home = "/",
    Cart = "/cart",
    Create = "/create",
    Account = "/account",
    Login = "/login",
    Signup = "/signup"
}

export enum ApiRoutes {
    Cart = "api/cart"
}